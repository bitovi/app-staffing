import { skills, projects, assignments, roles, employees } from "./fixtures";

describe("", () => {
  it("", async () => {
    skills;
    projects;
    assignments;
    roles;
    employees;

    // console.log(employees);
  });
});
