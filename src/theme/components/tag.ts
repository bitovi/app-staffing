import { StyleConfig } from "@chakra-ui/theme-tools";

const tag: StyleConfig = {
  baseStyle: {
    container: {
      backgroundColor: "gray.200",
      fontWeight: 700,
    },
    closeButton: {
      color: "red.500",
    },
    label: {},
  },
  variants: {},
  sizes: {},
};

export default tag;
